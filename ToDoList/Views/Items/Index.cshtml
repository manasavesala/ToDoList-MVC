<h1>List of items in category</h1>
@if (@Model.Count == 0)
{
  <h3>No Items have been added into category yet!</h3>
}

@foreach (var item in Model)
{
  <h3><a href='/categories/@category.GetId()/items/@item.GetId()'>@item.GetDescription() </a> </h3>
  
}

<p><a href='/'>Back home</a></p>